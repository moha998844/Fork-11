name: Fix tag change

on:
  workflow_dispatch:

permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
  steps:
  - name : install neo fetch
   run  : | 
         sudo apt-get update 
         

 
